# "mock" runs the whole UI on in-memory fixtures driven by the real SLA engine,
# with no backend and no database. "http" talks to the Express API.
NEXT_PUBLIC_API_MODE=mock
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
